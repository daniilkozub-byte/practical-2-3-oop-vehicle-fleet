# Практичне заняття № 2/3

**Розробка системи обліку військової техніки із застосуванням принципів ООП у Java**

## Формат

Розраховано на **одну пару** (≈80 хв). Це звичайний Java-проєкт із кількох `.java`-файлів — без Maven, без пакетів (`package`), без жодних білд-систем. Усе, що потрібно — JDK та текстовий редактор.

## Мета роботи

Закріпити практичні навички застосування основних принципів ООП у Java:

| # | Тема |
|---|------|
| 1 | Класи та об'єкти |
| 2 | Інкапсуляція |
| 3 | Абстракція |
| 4 | Наслідування |
| 5 | Перевизначення методів, поліморфізм |
| 6 | `toString()` |
| 7 | `equals()` та `hashCode()` |
| 8 | ★ Перевантаження методів (бонус, якщо лишиться час) |

## Загальна постановка задачі

Розробити невеликий консольний Java-застосунок для обліку військової техніки підрозділу: створювати різні типи техніки, зберігати інформацію про неї та працювати з усіма типами через спільний базовий тип.

> Усі кроки виконуються в **одному проєкті* — кожен наступний крок розширює те, що зроблено на попередньому.

Повний покроковий план з поясненнями — у **[ROADMAP.md](ROADMAP.md)**.

## З чого почати

1. Створіть на комП'ютері порожню папку (наприклад, `vehicle-fleet`) — це і буде ваш проєкт.
2. Створюйте в н�mй звичайні `.java`-файли (`Main.java`, `Vehicle.java` тощо) без `package` на початку файлу.
3. Компіляція та запуск з командного рядка:

   ```bash
   javac *.java
   java Main
   ```

   Або відкрийте папку як проєкт у будь-якій IDE (IntelliJ IDEA / Eclipse / VS Code) — вони 0�дніх за пропонують запусл.
4. Рухайтесь по кроках з [ROADMAP.md](ROADMAP.md) послідовно.

## Структура репозиторію

```text
practical-2-3-oop-vehicle-fleet/
├── README.md     — цей файл
└── ROADMAP.md     — покроковий план з поясненнями
```

Готового коду тут немає навмисно — весь код ви пишете самостійно за роадмапою.

## Результат роботи

Невеликий консольний Java-проєкт, у якому працюють: `private`-поля, конструктор, абстрактний клас і метод, наслідування, р@Override`, поліморфізм, `toString()`, `equals()` та `hashCode()`.
